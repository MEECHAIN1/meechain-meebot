# ⚡ MeeChain MeeBot

MeeChain MeeBot คือ DApp ที่ออกแบบมาเพื่อให้ผู้ร่วมพัฒนาและผู้ใช้สามารถ:
- ตรวจสอบยอดคงเหลือ Token และ NFT
- วางเดิมพัน (Stake) และรับรางวัล (Claim)
- ติดตาม Event Logs แบบเรียลไทม์
- แสดง NFT Gallery ของผู้ใช้

---

## 🚀 การติดตั้งและเริ่มต้นใช้งาน

### 1. Clone โปรเจกต์
```bash
git clone https://github.com/meechain1/meechain-meebot.git
cd meechain-meebot
```

### 2. ติดตั้ง dependencies
```bash
npm install
```

### 3. ตั้งค่า Environment Variables
ในสภาพแวดล้อมปัจจุบันที่รันโดยตรงในเบราว์เซอร์, Environment Variables จะถูกกำหนดใน `index.html` ผ่าน Global `window` Variables:

```html
<script>
  window.VITE_RPC_URL = "http://127.0.0.1:8545"; // Default Hardhat RPC URL
  window.VITE_NFT_ADDRESS = "0x5FbDB2315678afecb367f032d93F642f64180aa3"; // REPLACE WITH YOUR ACTUAL DEPLOYED CONTRACT ADDRESSES
  window.VITE_TOKEN_ADDRESS = "0xCf7Ed3AccA5a467e9e704C703E8D87F634fEAb28";
  window.VITE_STAKING_ADDRESS = "0xDc64a140Aa3E981100a9becA4E685f962f0cF6C9";
</script>
```
**สำหรับ Deployment:** คุณจะต้องตั้งค่า Environment Variables เหล่านี้ใน Dashboard ของ Vercel/Netlify (หรือแพลตฟอร์ม CI/CD อื่นๆ) โดยใช้ชื่อเดียวกัน (`VITE_RPC_URL`, `VITE_NFT_ADDRESS`, `VITE_TOKEN_ADDRESS`, `VITE_STAKING_ADDRESS`)

> ตรวจสอบให้แน่ใจว่าใช้ **checksum address** (ตัวพิมพ์ใหญ่/เล็กตรงตามจริง) เพื่อป้องกัน error

### 4. รันแอป
```bash
# ไม่มีคำสั่ง dev/start ในโมเดลนี้
# คุณสามารถเปิดไฟล์ index.html ในเบราว์เซอร์ได้โดยตรง
# หากคุณใช้ Live Server extension ใน VS Code: คลิกขวาที่ index.html แล้วเลือก "Open with Live Server"
```

---

## 📂 โครงสร้างไฟล์

```
src/
  abi/                # Minimal ABIs
  lib/
    contracts.ts      # Address + ABI map
    services/         # Token/NFT/Staking services
    viemClient.ts     # RPC client config
  context/
    AppState.ts       # Zustand global state
  components/
    Navbar.tsx
    BalanceCard.tsx
    EventStream.tsx
  pages/
    DashboardPage.tsx
    StakingPage.tsx
    GalleryPage.tsx
    EventLogPage.tsx
```

---

## 🧪 การทดสอบ

### Unit Tests
เนื่องจากเราไม่ได้ใช้ Vite หรือ create-react-app ในสภาพแวดล้อมนี้ จึงไม่มีคำสั่ง `npm run test` โดยตรง
การทดสอบจะเน้นไปที่การตรวจสอบด้วยตนเองและการตรวจสอบโค้ด:
- **Manual Testing:** ตรวจสอบฟังก์ชันการทำงานของ services (`token.ts`, `nft.ts`, `staking.ts`) โดยการเชื่อมต่อกับ Local Hardhat Network และทดลองเรียกใช้ฟังก์ชันต่างๆ ผ่าน UI
- **Code Review:** ตรวจสอบความถูกต้องของ logic และ Type Safety

### Integration Tests
- **Manual UI Verification:** ตรวจสอบว่า pages (`DashboardPage`, `StakingPage`, `GalleryPage`, `EventLogPage`) แสดงผลได้อย่างถูกต้องและโต้ตอบกับ Wallet/Contract ได้ตามที่คาดหวัง

### E2E Tests
- สำหรับการทดสอบแบบ End-to-End คุณจะต้องตั้งค่าเฟรมเวิร์กแยกต่างหาก (เช่น Playwright หรือ Cypress) ในสภาพแวดล้อมที่มี Build Tool (เช่น Vite) เพื่อจำลอง flow จริง: connect wallet → stake → claim → event log

---

## 🌐 Deployment Pipeline (Vercel/Netlify)

การ Deploy MeeChain MeeBot ไปยังแพลตฟอร์มอย่าง Vercel หรือ Netlify จะเป็นไปอย่างราบรื่น เนื่องจากแพลตฟอร์มเหล่านี้รองรับโปรเจกต์ React ที่ใช้ Native ES Modules ได้ดี

### 1. เตรียม Environment Variables
- เข้าสู่ Dashboard ของ Vercel/Netlify
- ไปที่ Project Settings → Environment Variables
- เพิ่มตัวแปรต่อไปนี้:
  - `VITE_RPC_URL`: URL ของ RPC ที่คุณต้องการใช้ (เช่น Sepolia, Optimism, หรือ RPC สาธารณะอื่นๆ)
  - `VITE_NFT_ADDRESS`: Address ของ NFT contract ที่ Deploy บน Testnet/Mainnet
  - `VITE_TOKEN_ADDRESS`: Address ของ ERC20 token contract ที่ Deploy บน Testnet/Mainnet
  - `VITE_STAKING_ADDRESS`: Address ของ Staking contract ที่ Deploy บน Testnet/Mainnet

> **สำคัญ:** ตรวจสอบให้แน่ใจว่าใช้ **checksum address** (ตัวพิมพ์ใหญ่/เล็กตรงตามจริง) เพื่อป้องกัน error ในการโต้ตอบกับ Smart Contract

### 2. Build & CI/CD
- **เชื่อมต่อ Repository:** เชื่อมต่อโปรเจกต์ GitHub/GitLab ของคุณกับ Vercel/Netlify
- **Build Command:** สำหรับโปรเจกต์ที่ใช้ Native ES Modules แบบนี้ ปกติแล้ว Vercel/Netlify จะสามารถตรวจจับและ Build ได้โดยอัตโนมัติ โดยอาจใช้คำสั่งเช่น `npm run build` (หากคุณเพิ่ม script นี้ใน `package.json` สำหรับการ bundling) หรือปล่อยให้แพลตฟอร์มจัดการเอง
- **CI/CD Automation:** ทุกครั้งที่คุณ `git push` โค้ดไปยัง branch หลัก (เช่น `main` หรือ `master`), Vercel/Netlify จะ Trigger การ Build และ Deploy แอปพลิเคชันของคุณโดยอัตโนมัติ

### 3. Smoke Tests หลัง Deploy
หลังจาก Deploy สำเร็จ โปรดทำการทดสอบตามเช็คลิสต์ต่อไปนี้:
- **การโหลดแอปพลิเคชัน:** เปิดแอปในเบราว์เซอร์ ตรวจสอบว่าโหลดได้สมบูรณ์ ไม่มีข้อผิดพลาดใน Console
- **Navbar:** เมนู Dashboard, Staking, Gallery, Event Logs แสดงครบถ้วนและสามารถคลิกไปได้แต่ละหน้า
- **DashboardPage:** แสดง Token Balance, NFT Balance, Reward Rate และข้อมูลอื่นๆ ได้ถูกต้อง
- **StakingPage:** สามารถเชื่อมต่อ Wallet, Approve NFT, Stake NFT, Unstake NFT และ Claim Rewards ได้ (โดยใช้ Testnet / Wallet ที่มี Test Tokens/NFTs)
- **GalleryPage:** ดึงข้อมูลและแสดงการ์ด NFT ของผู้ใช้ได้ถูกต้อง
- **EventLogPage:** Subscribe และแสดง Event Logs แบบ Real-time ได้ (ลองทำธุรกรรมจริงเพื่อดูผล)
- **Error Handling:** ทดสอบสถานการณ์ที่เกิดข้อผิดพลาด (เช่น RPC Down, Transaction Fail) เพื่อให้แน่ใจว่า Global Error Banner แสดงผลอย่างถูกต้อง
- **Contributor Experience:** ตรวจสอบ Loading Indicator, Confetti Celebration และ Emoji Logs ว่าทำงานได้ตามที่คาดไว้

### 4. Contributor Experience
- **Global Error Banner:** แสดง ❌ เมื่อ RPC/contract call fail
- **Loading Indicator:** แสดง ⏳ ระหว่าง fetch data
- **Celebration Rituals:** confetti + emoji logs เมื่อ stake/claim สำเร็จ
- **Export Logs:** ปุ่ม export JSON → contributor ใช้งานง่าย

---

## 🤝 การมีส่วนร่วม

1.  Fork โปรเจกต์
2.  สร้าง branch ใหม่: `git checkout -b feature/your-feature-name`
3.  Commit การเปลี่ยนแปลงของคุณ: `git commit -m "feat: Add your amazing feature"` (ใช้ mystical commit template)
4.  Push ไปยัง branch ของคุณ: `git push origin feature/your-feature-name`
5.  เปิด Pull Request ไปยัง `main` branch
6.  ร่วมฉลอง milestone 🎉!
